// Base
import React from 'react'

import './spinner.css'

const Spinner = () => {
  return (
    <div className="wrSpinner">
      <span>Loading data...</span>
    </div>
  )
}

export default Spinner
